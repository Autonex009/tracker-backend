// electron-app/preload.js
// No exposed APIs required for this demo, but we keep contextIsolation enabled.
window.addEventListener('DOMContentLoaded', () => {
  // could expose safe APIs via contextBridge if needed
});
